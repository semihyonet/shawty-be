# Set the max memory usage (e.g., 100mb)
maxmemory 100mb;

# Set the eviction policy to LRU
maxmemory-policy allkeys-lru;

# Other Redis settings you may want to configure
# For example, setting the append-only file (AOF) mode for persistence
appendonly yes;
